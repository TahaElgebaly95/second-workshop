import 'package:flutter/material.dart';

class Fifteen extends StatelessWidget {
  const Fifteen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Column(
          children: [
            Image.asset('assets/images/15_login'),
          ],
        ),
    );
  }
}
